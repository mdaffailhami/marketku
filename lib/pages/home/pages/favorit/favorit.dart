import 'package:flutter/material.dart';

class MyFavoritPage extends StatelessWidget {
  const MyFavoritPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('FAVORIT'));
  }
}
