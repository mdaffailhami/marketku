import 'package:flutter/material.dart';

class MyPencarianPage extends StatelessWidget {
  const MyPencarianPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('PENCARIAN')),
    );
  }
}
